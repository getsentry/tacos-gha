# this function is completely handled by tacos-plan
name: TACOS Lock (deleteme)

# downstream projects should use events:
###pull_request:
###  types: [opened, synchronize, reopened]

on:
  workflow_call:
    inputs:
      tacos_gha_repo:
        type: string
        default: getsentry/tacos-gha
      tacos_gha_ref:
        type: string
        default: refs/heads/stable

jobs:
  noop:
    runs-on: ubuntu-latest
    steps: []
