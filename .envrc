export REPO_TOP="$PWD"


export VIRTUAL_ENV="$PWD/venv"
path_add PATH "$PWD/bin" "$VIRTUAL_ENV/bin"

# for use in your PS1 definition (see ~/.zshrc)
export PS1_PREFIX="(venv:$(basename $PWD)) ${PS1_PREFIX:-}"
export NPROC="${NPROC:-$(getconf _NPROCESSORS_CONF)}"
