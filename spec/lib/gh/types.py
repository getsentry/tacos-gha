from __future__ import annotations

from lib.types import URL as URL
from lib.types import Generator as Generator

# FIXME: use a more specific type than str
Branch = object
Message = object
Label = str
CheckName = str
WorkflowName = str
